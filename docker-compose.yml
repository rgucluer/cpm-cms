services:
  payload:
    container_name: payload
    restart: unless-stopped
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    pull_policy: build
    image: custom-payload-website:3.85
    build:
      context: .
      dockerfile: Dockerfile
      network: host
    environment:
      - 'NODE_ENV=production'
    healthcheck:
      test: 'curl -f --head http://127.0.0.1:3000 || exit 1'
      # test: 'true'
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
    expose:
      - 3000 
    working_dir: /app/
    # command: 'tail -f /dev/null'
    command: 'sh -c "node server.js"'
    labels:
      traefik.enable: true
      coolify.managed: true
      coolify.proxy: true


